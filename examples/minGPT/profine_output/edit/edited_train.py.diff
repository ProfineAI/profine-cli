--- original+++ optimized@@ -1,7 +1,3 @@-"""
-Trains a character-level language model.
-"""
-
 import os
 import sys
 
@@ -12,6 +8,8 @@ from mingpt.model import GPT
 from mingpt.trainer import Trainer
 from mingpt.utils import set_seed, setup_logging, CfgNode as CN
+
+torch.set_float32_matmul_precision('high') # profine: tf32_matmul
 
 # -----------------------------------------------------------------------------
 
@@ -100,6 +98,7 @@     config.model.vocab_size = train_dataset.get_vocab_size()
     config.model.block_size = train_dataset.get_block_size()
     model = GPT(config.model)
+    model = torch.compile(model, mode='max-autotune') # profine: torch.compile_max_autotune
 
     # construct the trainer object
     trainer = Trainer(config.trainer, model, train_dataset)
